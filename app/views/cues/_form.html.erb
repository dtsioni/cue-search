<%= form_for(@cue) do |f| %>
  <% if @cue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cue.errors.count, "error") %> prohibited this cue from being saved:</h2>

      <ul>
      <% @cue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :artist_name %><br>
    <%= f.text_field :artist_name %>
  </div>
  <div class="field">
    <%= f.label :album_name %><br>
    <%= f.text_field :album_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
